// Convert winBattle to an arrow function
const winBattle = () => true;

// Create the experiencePoints variable using a ternary operator
const experiencePoints = winBattle() ? 10 : 1;

// Display the result
console.log(experiencePoints); 
